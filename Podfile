# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SeptaMobile' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SeptaMobile
  pod 'Firebase/Analytics'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'GoogleSignIn'
  pod 'Firebase/Firestore'

  target 'SeptaMobileTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SeptaMobileUITests' do
    # Pods for testing
  end

end
